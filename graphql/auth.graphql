extend type Mutation {

    register(input: RegisterInput!): RegisterResult!
    verifyAccount(input: VerifyAccountInput!): Result!
    login(input: LoginInput!): LoginResult!
    logout: Result @guard
    
}